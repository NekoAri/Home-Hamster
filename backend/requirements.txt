# ============================================================
# HomeHamster 后端依赖
# 安装: pip install -r requirements.txt
# ============================================================

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1

# 数据库
asyncpg==0.30.0              # PostgreSQL 异步驱动 (0.30+ 支持 Python 3.13)
pgvector==0.3.0              # pgvector Python 支持
pg8000==1.31.2               # 纯 Python PostgreSQL 驱动（用于 init_db.py 初始化脚本，兼容 GBK 服务器）

# 配置管理
pydantic==2.8.2
pydantic-settings==2.4.0

# LLM
openai>=1.54.0               # OpenAI SDK (支持 Function Calling & 流式输出，兼容 Ollama/DeepSeek 等，1.54+ 兼容 httpx 0.28)
anthropic>=0.34.2           # Anthropic Claude SDK

# 工具
python-dotenv==1.0.1
